# Portable WikiSkill acceptance matrix

Status: PROPOSED FOR GATE A. All 27 source-derived HF-A requirements are present. `Required proof` defines Engineering's future evidence; it does not claim the proof exists now.

| ID | Requirement preserved | Core disposition | Hermes adapter | Claude adapter | Codex adapter | Required proof and pass criterion |
|---|---|---|---|---|---|---|
| HF-A01 | Revision 8 remains `NEEDS REVISION`; no R8 plan review and no `SOUND`. | Provenance status is immutable. | Display and handoff preserve status. | Projection preserves status. | Review runner preserves status. | Search every contract, report, package, and UI status. Pass only with no claim that R8 is validated, SOUND, implemented, or shipped. Cite R7 322-336 and absence of R8 report. |
| HF-A02 | Distinguish paper facts, direct decisions, Claude proposals, Codex claims, architecture, and runtime evidence. | Every requirement/artifact has `source_class` and locator. | Adds `RUNTIME` records only. | Adds `RUNTIME` records only. | Adds `RUNTIME` or original `CODEX` records only. | Schema rejects a normative requirement without class and locator. Fixture proves classes remain distinct after serialization and packaging. |
| HF-A03 | Original reports govern Codex finding identity and closure history. | Original report handle and locator are primary; later response is separate. | No reinterpretation. | No reinterpretation. | Gate reports reference origin and later plan text separately. | Audit all Codex finding rows. Pass only if each originated finding cites R1-R7 or rebuttal and no plan summary replaces its closure state. |
| HF-A04 | Preserve empty baseline, strict aggregate `>`, skill-only rollback, persistent wiki, and impact history as paper facts. | Lifecycle owns all five invariants. | Dispatches transitions. | Dispatches transitions. | Dispatches transitions. | Fixtures: empty baseline; tie rejection; strict win; rejection keeps active snapshot and wiki hash; accepted and rejected impact entries persist. Confirmation is labelled local. |
| HF-A05 | Paper-complete traces and narrower observable transcripts cannot share one fidelity label. | Capture taxonomy has `paper_complete`, `observable_transcript`, `final_only`, `unknown`. | Default observed label is `observable_transcript`, `unsandboxed` until stronger proof. | Same default. | Same default unless sandbox/capture proof passes. | Capability fixture enumerates captured events. Pass only if missing reasoning/tool events prevents `paper_complete`; package displays actual label. |
| HF-A06 | Do not claim exact `read_file`/`finish` for every paper role. | Role/tool evidence is role-specific. | Hermes tools described from runtime evidence. | Claude metadata described from runtime evidence. | Codex tools described from runtime evidence. | Static claim scan plus role matrix. Pass only if exact pair is attributed to Proposer evidence and no adapter promotes it to all roles. |
| HF-A07 | Prove role, no-wiki, and held-out-answer isolation per runtime or label procedural/unsandboxed. | Capability report separates five isolation dimensions. | Negative access test or procedural label. | Negative access test or procedural label. | Negative access test or procedural label. | For each runtime, attempt forbidden wiki and held-out-answer reads under exact launch config. Any success, skipped test, or unknown result forces `unsandboxed` and forbids `unseen`. |
| HF-A08 | Bind prediction to recorded output; extractor/scorer failure invalidates evidence and never becomes a score. | Captured output hash, extractor result, scorer result, and manifest validity are bound. | Returns captured output record. | Returns captured output record. | Returns captured output record. | Fixtures cover timeout, nonzero, empty, multiline/ambiguous, NaN, infinity, and out-of-range. Pass only if all produce invalid manifest, no score, no phase advance. |
| HF-A09 | State machine is total across normal, invalid, reset, abandon, confirmation, terminal, and delivery branches. | Explicit state and operation table with refusal for every other pair. | Wrapper parity. | Wrapper parity. | Wrapper parity. | Generate cross-product of states and operations. Pass only if every pair has one deterministic transition/refusal and branch fixtures reach expected terminal state. |
| HF-A10 | Resolve unparseable-train/reset/raw ownership before implementation. | Reset deletes manifest, sidecars, raw traces, and aliases for the corrupt train phase; keeps submitted `.out.md`. | No adapter ownership. | No adapter ownership. | No adapter ownership. | Fixture: valid train publishes raw, manifest is corrupted, reset runs, all derived train artifacts disappear, corrected output re-ingests without collision, hashes match a clean corrected run. |
| HF-A11 | Transactions are self-contained and recovery equality covers every owned root and residue. | Intent-first transaction record, immutable replay inputs, deterministic temps, complete equality oracle. | Cannot bypass core transaction. | Cannot bypass core transaction. | Cannot bypass core transaction. | Crash at pre-intent, post-intent, each first/mid/last output, publication, and precommit boundary. Pass only if recover twice is idempotent and equals uninterrupted across domain, snapshots, raw, wiki, staging, archive, route, ledger, with no extras. |
| HF-A12 | Snapshot, pointer, and mirror source of truth is unambiguous and hash verified. | Active pointer is sole authority; snapshots immutable; mirror disposable. | Reads through core. | Reads through core. | Reads through core. | Tests: corrupt snapshot refuses; pointer switch atomic; deleted/modified/extra mirror rebuilds lazily; marker does not affect hash; failed gate never changes active pointer. |
| HF-A13 | Use paper sampling limits and label deterministic ordering as local. | Enforces 5 failures, 3 passes, 15,000 chars; sorted ID selection labelled `ARCHITECTURE`. | None. | None. | None. | Boundary and repeat tests prove limits, source hashes, byte-idempotence, and deterministic order. Documentation cites paper for limits and architecture for ordering. |
| HF-A14 | Maintainer excludes ground-truth list; Proposer receives train outcomes including ground truth. | Separate role-input schemas. | Job specs omit/allow exact fields. | Job specs omit/allow exact fields. | Job specs omit/allow exact fields. | Schema and leakage tests: Maintainer payload has no expected/marker; Proposer has train expected only; neither gets val/test expected or answer paths. Forbidden field causes adapter/core rejection. |
| HF-A15 | Separate mechanical pattern grammar from semantic judgments. | Parser verifies structure/provenance; judgment fields remain attestations. | Role emits judgments. | Role emits judgments. | Role emits judgments. | Parser fixtures cover headings, lines, tuples, patch uniqueness, class disposition, and provenance. Claim scan forbids mechanically-proven commandness, root cause, semantic dedup, or generalizability. |
| HF-A16 | Add missing-class, blank-reason, duplicate-tuple, and trailing-JSON cases; use multiset consistently. | Gate 1 inventory explicitly contains all four and multiset equality. | None. | None. | None. | Four dedicated negative fixtures plus duplicate-count positive/negative cases. Pass only if each fails without mutation and all docs/tests say multiset, not set. |
| HF-A17 | Markers detect propagation, not access; collision-check and scan before publication. | Marker generation, collision scan, artifact policy, candidate prepublication scan. | Captures outputs for scan. | Captures outputs for scan. | Captures outputs for scan. | Tests: source collision rejected; legal correct prediction equal to expected passes; copied marked record rejected; marker never in prediction artifacts/stdout; marker-free paraphrase is explicitly not detected. |
| HF-A18 | Enumerate dynamic inputs, outputs, state, args, negative existence, clock, routes, and replay sources. | Versioned dependency matrix drives drift checks. | Adds runtime job spec, capability report, and capture as adapter inputs. | Same. | Same. | One mutation fixture per dependency cell. Pass only if drift is refused before mutation; omitted dependency fails a coverage meta-test. |
| HF-A19 | Validated and untested routes are durable, exclusive, and separately labelled. | State owns route and ledger; first route action latches; opposite route refuses. | Human approval bridge. | Human approval bridge. | Human approval bridge. | Bidirectional fixtures: test prepare blocks untested; untested blocks test; no test artifacts on untested route; four untested labels; ledger identity/idempotency; approval record required. |
| HF-A20 | Archive members are safe normalized files and staged/archive canonical path-byte hashes match. | Canonical package and archive builder. | Stages Hermes projection. | Stages Claude projection. | Stages Codex projection. | Reject absolute, backslash, empty, dot/dotdot, duplicate, directory, symlink, extra, and missing members. Pass only if unique regular POSIX files exactly equal staged membership and tree hashes match; tamper detected. |
| HF-A21 | Gate 2 fails deterministic binding mismatch; nondeterministic LLM differences are report-only unless fixture-governed. | Comparator classifies fields by verdict policy. | Supplies run evidence. | Supplies run evidence. | Runs independent review. | Deterministic mismatch fixtures each fail. Fresh score/impact/snapshot differences report without failure unless tied to deterministic fixture. Comparator policy itself is hash-bound and reviewed. |
| HF-A22 | Resolve nonexistent observation-seeding transition. | Adds one-time `NEEDS_OPTIONAL_SEED` with `seed-observations` or `skip-seed`. | Captures named human approval and IDs. | Same projection. | Same projection. | Fixtures: no seed; valid named seed; internal observation into public domain refused; repeat seed refused; crash/recover equality; rollback before baseline; provenance inherited into cited skill PURPOSE. |
| HF-A23 | Task Observer and WikiSkill gates remain distinct; promotion is not acceptance or installation. | Separate provenance and phase gates. | `skill_manage` never called live by evolution; later staged review only. | No direct install. | No direct install. | Integration test confirms WikiSkill DONE/export leaves Task Observer and all live skill roots byte-identical. Package status says staged candidate, not installed/accepted by Task Observer. |
| HF-A24 | Reject implicit live-session capture; raw evidence comes from declared task sets. | Ingest accepts only declared task/answer manifests and optional named observation seed packet. | No transcript-mining route. | No transcript-mining route. | No transcript-mining route. | API/schema has no session-history source. Negative test passes session/transcript handle and is refused. Search built package for transcript-mining procedure. |
| HF-A25 | Trusted text scope, confirmation, transaction model, and delivery controls are proposals requiring explicit adoption/change/rejection. | Decision log records A2/A3 and architecture changes. | Obeys approved values. | Obeys approved values. | Obeys approved values. | Gate A record must explicitly decide A2 and A3; transaction and delivery rows cite `ARCHITECTURE`; no item is attributed to DIRECT or PAPER unless sourced. Missing decision blocks Engineering. |
| HF-A26 | Separate paper three-run/bootstrap evidence from local confirmation. | Claim policy has `local_acceptance` and `paper_comparable_public_claim` classes. | Report labels. | Report labels. | Report labels. | Local smoke can pass with selected A2 rule but cannot produce paper-comparable claim. Public-claim fixture requires three complete runs plus paired-bootstrap evidence or emits explicit non-comparable disclaimer. |
| HF-A27 | Fresh approval is required before architecture-dependent state-changing phases; historical plan authorizes none. | Approval records are typed, scoped, expiring/one-phase, and hash-bound. | Kanban/human approval surfaces translate records. | Runtime approval plus human record. | Runtime approval plus human record. | Gate tests reject missing, stale, wrong-hash, wrong-runtime, wrong-destination, or reused approval. Separate records required for implementation, each install, and publication/distribution. Historical transcript/plan never satisfies a gate. |

## Gate A criterion matrix

| Gate A criterion | Required answer | Recommended default | Blocking consequence if unanswered |
|---|---|---|---|
| GA-01 Fidelity floor | Narrow truthfully labelled evidence allowed, or paper-complete/enforced only | Allow narrower with `observable_transcript` and `unsandboxed`; reserve stronger claims for proof | Adapter execution cannot be scoped |
| GA-02 Local acceptance | Local gate and public-comparison evidence policy | Strict aggregate win plus fresh confirmation locally; three-run/bootstrap for paper-comparable public claim | Evaluation lifecycle cannot be implemented |
| GA-03 Portable v1 scope | Trusted text-only `none|read`, or broader domain classes | Approve narrow v1 | Schemas, threat model, and test matrix remain open |
| GA-04 Licensing | New code/docs licenses and verbatim prompt redistribution | MIT code; CC BY 4.0 docs/methodology; separately attributed prompt appendix | Package and any later distribution remain blocked |
| GA-05 Implementation | Approve, revise, or reject bounded Engineering scope | Core plus Hermes adapter only after independent contract review | No code work may begin |

## Gate A pass checklist

- [ ] GA-01 through GA-05 each have one explicit recorded decision.
- [ ] The approval record includes SHA-256 of `architecture-contract.md`, `source-parity-matrix.md`, and this file.
- [ ] The approval record states whether it authorizes Engineering; silence means no.
- [ ] The approval record does not authorize installation, publication, or distribution.
- [ ] HF-A01 through HF-A27 are independently reviewed for criterion fidelity.
- [ ] SP-001 through SP-106 are independently reviewed for complete Revision 8 coverage.
- [ ] The four Revision 8 contradictions have explicit approved dispositions: intent-first transaction, corrupt-train raw reset, complete Gate 1 class/multiset fixtures, and real seed transition.
- [ ] OpenAI-only provider policy and no-paid-services policy are accepted as implementation constraints.
- [ ] Claude executable adapter is acknowledged as dormant while OpenAI-only routing cannot be demonstrated.
- [ ] Task Observer installed copies remain out of implementation write scope.
- [ ] Live Hermes, Claude, and Codex skill/config roots remain out of implementation write scope.
- [ ] An independent Architect review is scheduled before Engineering dispatch.

## Current evidence state

Architecture mapping: complete for review.
Implementation evidence: absent by design.
Runtime conformance evidence: absent by design.
Installation evidence: absent.
Publication or distribution evidence: absent.
