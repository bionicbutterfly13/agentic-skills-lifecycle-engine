---
phase: quick
plan: 260906-tz7
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_distribution.py
  - tests/test_workspace_hermes_adapter.py
  - MANIFEST.in
  - setup.py
  - scripts/verify_distribution.py
  - docs/distribution.md
  - .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-EVIDENCE.json
  - .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-SUMMARY.md
autonomous: true
requirements:
  - PORT-01
must_haves:
  truths:
    - "A source distribution contains every declared public source file, complete inherited runtime and evaluation fixtures, tests, license material and required document dependencies."
    - "Project distributions exclude explicitly classified internal planning and historical local-path audit records while preserving their canonical bytes in the repository."
    - "Generic candidate projection and archive scanners retain their existing behavior, including rejection of private content in candidate docs/evidence and docs/research."
    - "The declared PEP 517 build produces verified source and wheel archives, and the source archive can rebuild the same runtime and skill payload without Git or internal planning files."
    - "Every inherited file remains attributable to the verified import commit; intentional packaging changes are recorded separately from the immutable original import receipt."
  artifacts:
    - path: MANIFEST.in
      provides: "Explicit public source distribution membership consumed by setuptools and checked independently"
    - path: scripts/verify_distribution.py
      provides: "Standard-library inventory, source classification and complete archive verification, shared by build hooks and checks"
    - path: tests/test_distribution.py
      provides: "Adversarial membership, scan, archive, rebuild and preservation regressions"
    - path: docs/distribution.md
      provides: "Public distribution contract, build/check commands and evidence limits"
    - path: .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-EVIDENCE.json
      provides: "Exact commands, import checkpoint, full member inventories, hashes and preserved internal-record identities"
  key_links:
    - from: setup.py
      to: scripts/verify_distribution.py
      via: "Existing build_py and a setuptools sdist command validate project source and produced release-tree bytes"
    - from: scripts/verify_distribution.py
      to: src/asme/package.py
      via: "Calls unchanged safe_member_name and scan_community_safety on complete selected payloads and archive members"
    - from: tests/test_workspace_hermes_adapter.py
      to: scripts/verify_distribution.py
      via: "Expanded project distribution positive check alongside the retained generic-projection negative check"
---

<objective>
Define and verify Lifecycle's explicit public source distribution boundary so the exact ASME import and subsequent historical WikiSkill audit can coexist without shipping private operational records or losing required package content.

Purpose: close PORT-01's separate package-boundary criterion while retaining the existing candidate-package refusal contract. The observed source archive omitted all 14 arithmetic and echo evaluation files; repair that completeness defect as part of this boundary.
Output: one separately reviewed packaging feature, complete real build evidence and an attributable post-import change record. This task does not claim WikiSkill method or empirical parity.
</objective>

<execution_context>
@/Users/manisaintvictor/.claude/gsd-core/workflows/execute-plan.md
@/Users/manisaintvictor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@pyproject.toml
@setup.py
@MANIFEST.in
@src/asme/package.py
@src/asme/skill_assets.py
@src/asme/source_registry.py
@tests/test_workspace_hermes_adapter.py
@tests/test_release_surface.py
@tests/test_skill_assets.py
@tests/test_snapshot_package.py
@scripts/verify_asme_port.py
@docs/migration/asme-source-manifest.json
@docs/migration/asme-source/CONTRIBUTING.md

Planning-time source observation: runtime files above were read only in the existing sibling port worktree, pinned to original ASME revision 1137c6705fd844546d5588757a5a23d4007b20c4. This branch currently starts at integration revision 19375806a748db16e706c70f49aa3bc83b142eb0 and does not yet contain the port. References become local execution inputs only after the prerequisite below passes. Do not copy runtime files from that worktree.

Scope authority is the adopted autonomous WikiSkill-v1 goal and the canonical sibling audit worktree's .planning/workstreams/wikiskill-v1/REQUIREMENTS.md and ROADMAP.md, committed in 339515eddd3fe5698aa1c80a33184bbbcc67b507. Phase 1 criterion 4 assigns this feature to PORT-01. Those files are read-only inputs until their separate integration; do not create a duplicate workstream here. WS-M01 and other phase requirements remain assigned to their existing work, not this quick task.

<interfaces>
Existing scanner interfaces in src/asme/package.py:
- scan_community_safety(files: Mapping[str, bytes]) -> None raises ContractError for unsafe member names, credential-like files/content or private absolute paths.
- build_projection(*, source_root: Path, compatibility: Compatibility, source_attribution: Sequence[Mapping[str, str]], generated_files: Mapping[str, bytes] | None = None, ...) -> Projection traverses the supplied candidate tree and applies that scanner.
- build_projection_from_files(*, files: Mapping[str, bytes], compatibility: Compatibility, source_attribution: Sequence[Mapping[str, str]], generated_files: Mapping[str, bytes] | None = None, ...) -> Projection uses the same safety contract.
- safe_member_name is defined in asme.canonical and imported therefrom by asme.package; reuse its path rules and add archive-type constraints locally rather than relaxing it.

Existing skill asset interfaces in src/asme/skill_assets.py:
- SKILL_FILES = (SKILL.md, PURPOSE.md, NOTICE.md, LICENSE); SKILL_DIRECTORIES = (references,); PACKAGED_SUBDIRECTORY = skill.
- skill_files(root: Path | None = None) -> tuple[str, ...] enumerates the exact wheel skill companions and rejects symlinks.
- copy_skill_tree(source_root: Path, destination: Path) -> tuple[str, ...] copies those canonical bytes.

Existing setup.py loads skill_assets by file path, subclasses setuptools.command.build_py.build_py, calls super().run(), and copies the skill tree into build_lib/asme/skill for non-editable builds. Preserve its editable behavior and wheel payload contract.

Proposed build-helper contracts to establish with regression tests:
- source_distribution_files(root: Path) -> dict[str, bytes]: validate the explicit manifest and exhaustive source classification, return the complete safe intended source payload.
- verify_sdist(archive: Path, *, source_root: Path) -> dict: validate every tar member, source equality and allowed generated metadata; return complete member/hash evidence.
- verify_wheel(archive: Path, *, source_root: Path) -> dict: validate every ZIP member, expected runtime/data/skill mapping and generated metadata including RECORD hashes; return complete member/hash evidence.
- main(argv=None): developer build-check command accepting --source, --artifacts and optional --check-source. This is not an asme runtime subcommand or a replacement build backend.

Checkout-local import contract: scripts/verify_distribution.py derives its own repository root from Path(__file__).resolve().parents[1], prepends that root's src directory before its first asme import, and imports safe_member_name from asme.canonical and scan_community_safety from asme.package. Refuse an already-loaded asme module with a file origin outside that local src/asme tree before reusing it; never silently fall back to an installed package. setup.py loads this helper by file path, as it already does for skill_assets, so both the build hook and standalone checker bootstrap the same local imports. This also works from an extracted sdist, which includes the helper and complete src/asme tree and does not require Git, an editable install, ambient PYTHONPATH or internal planning records.
</interfaces>
</context>

## Settled decisions

- D-01: Execute only on fix/portable-package-boundary in its task-owned linked worktree, after incorporating verified current main containing the completed compatibility port. Integration order is exact port, this feature, then audit/workstream. Preserve other sessions and root STATE.md.
- D-02: Keep src/asme/package.py, generic candidate CLI/functions, scanner patterns and candidate docs/evidence/docs/research behavior unchanged. A project distribution allowlist must never become a general scanner exemption.
- D-03: Keep canonical historical audit commands, source links, migration receipts, imported originals and license/registry anchors unchanged. Exclusion from a distribution is not permission to sanitize, rewrite, move or delete them.
- D-04: Keep package asme, CLI asme, distribution agent-skill-mastery-engine, version 0.2.0, Python >=3.11, declared setuptools>=68 build backend and current wheel skill payload. No new dependency or global installation is required.
- D-05: Establish complete public source membership and exact source classification, including all 14 existing arithmetic/echo evaluation files. Reject unknown, missing required, duplicate, escaping, symlink, hardlink and generated-collision cases. The same private canary in public runtime bytes must still fail the unchanged scanner.
- D-06: Run regression tests, actual source/wheel builds, complete archive checks, independent review and historical source accounting. Commit, merge and safe task-owned cleanup are authorized. Paid execution, publication and source deletion are not.

## Prerequisite and mutable-scope guard

Before Task 1, the orchestrator must supply the completed port's verified integration commit and passing review/check receipts. Re-read the live worktree list, branch, ancestry and task-owned status. Preserve/commit only this plan through the GSD orchestrator if needed, then incorporate verified current main using the repository's normal integration process. Do not base execution on the still-running port branch, import source yourself, or infer port completion from build success. Record the actual import/integration commit, manifest SHA256 and expected source revision in the quick evidence file. If the expected source files, namespace, manifest or contribution contract differ, report the exact mismatch before editing; the planning-time file list alone does not authorize unrelated changes.

At the same checkpoint record hashes and modes for the original import manifest, historical source documents, current attribution anchors and any present canonical audit files. The later audit integration will add the audit's exact docs/research/wikiskill-code-parity.md and docs/evidence/wikiskill-parity-audit.json. Test their exclusion using fixtures or an orchestrator-provided immutable snapshot; do not read from or copy their mutable branch into this feature.

## Verified discovery and package legitimacy

Discovery is bounded to the existing Python packaging surface. The inspected MANIFEST.in recursively includes docs Markdown and only assets matching *.tmpl. A real declared-backend build in the port lane used setuptools 84.0.0 and produced a 53-member wheel and 117-member source archive, with complete current runtime/skill wheel bytes but all 14 assets/eval files absent from the source archive. These counts are baseline observations, not target constants.

Setuptools 84.0.0's [sdist source](https://github.com/pypa/setuptools/blob/v84.0.0/setuptools/command/sdist.py#L57-L73) obtains egg_info.filelist then constructs the release. Its [make_release_tree implementation](https://github.com/pypa/setuptools/blob/v84.0.0/setuptools/command/sdist.py#L165-L175) accepts base_dir and files and adds generated setup.cfg after the inherited copy. Register an sdist subclass through the existing setup(cmdclass=...) surface; validate the selected list before copying and the complete release tree after super().make_release_tree(...). Do not assume validation before super covers generated metadata. [egg_info](https://github.com/pypa/setuptools/blob/v84.0.0/setuptools/command/egg_info.py#L313-L320) generates SOURCES.txt; stale or plugin-added entries must fail the project boundary rather than silently entering the artifact.

| Package | Use | Primary identity | Classification |
|---------|-----|------------------|----------------|
| setuptools | Already declared PEP 517 backend; no requirement change | Official pypa/setuptools source above, actual isolated build observed | Verified existing |
| build | Already available development frontend used by the orchestrator | python3 -m build succeeded in the supplied build receipt | Existing tool, no install task |

No package-manager installation task is planned. If a declared isolated build cannot resolve its existing backend, preserve the exact failure and report it. Do not replace the official build with a manual archive, change dependencies, or install globally.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish adversarial source and archive boundary regressions</name>
  <files>tests/test_distribution.py, tests/test_workspace_hermes_adapter.py</files>
  <behavior>
    - An explicit source inventory contains all runtime Python/data files, templates, adapters, scripts, complete inherited tests/fixtures, public source documents and their required relative document dependencies.
    - All 14 arithmetic/echo answers, cartridge, prompt, tasks and rollout files are present byte-for-byte in the source package; wheel skill membership remains unchanged.
    - A canonical private historical record under .planning or either exact audit file is retained byte-for-byte in the repository, omitted from the project payload and still rejected by generic full-root projection.
    - A private canary inserted into a public runtime file or an ordinary candidate docs/evidence or docs/research file fails the existing scanner; construct inert canaries from fragments so the test source is itself package-safe.
    - Manifest omissions, undeclared source files, unknown directives, duplicate entries, absent includes, unsafe paths, symlink traversal and attempts to classify runtime/assets/tests as internal fail.
    - Source and wheel archives reject duplicate names, unsafe normalized names, escaping root prefixes, symlinks/hardlinks or special members, missing required members, extra unknown members, altered source bytes and collisions with generated metadata.
    - Extracted source distributions work without Git or internal planning records; verification does not require either to exist and does not fabricate their preservation evidence.
  </behavior>
  <action>After the prerequisite passes, add focused tests against the proposed helper contracts per D-02 through D-05. Expand the existing test_current_tree_is_community_scan_clean into explicit generic-negative and project-distribution-positive assertions while retaining its useful community-safety guarantee. Exercise the actual current public source payload, and use an isolated temporary full source fixture with a known private internal canary for the generic rejection assertion so the test remains valid in an extracted source archive. Verify actual canonical private audit records when present as well; their absence in an sdist is expected and must not skip the fixture check. Do not drop the test, mark it xfail, exclude it from the suite or catch arbitrary errors. Keep the ordinary candidate tests covering docs/evidence and docs/research so there is no directory-wide exemption. Archive membership assertions must independently enumerate expected runtime, skill, fixture and metadata families rather than compare the helper with itself or use PUBLIC_ARTIFACTS alone. Record an actual RED run before implementing the helper.</action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider tests/test_distribution.py tests/test_workspace_hermes_adapter.py -k 'distribution or community_scan' -q</automated>
  </verify>
  <done>The newly specified boundary tests fail for the missing helper/current package omissions with recorded failures. Existing scanner assertions remain intact. Every listed adversarial behavior has a named test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire a complete explicit inventory into the declared build path</name>
  <files>MANIFEST.in, setup.py, scripts/verify_distribution.py, docs/distribution.md</files>
  <action>Create docs/distribution.md with the public distribution contract and developer commands before declaring it in MANIFEST.in and before either GREEN verification command; Task 3 appends actual verification results. Implement the tested helper contracts using only the standard library and unchanged asme path/safety utilities, per D-02 through D-05. Make MANIFEST.in the single public source-membership authority: clear implicit source matches with the supported global-exclude directive, then enumerate literal include paths with no recursive wildcard admission. The helper must accept only this documented grammar, reject duplicates and unsafe/missing paths, and check completeness independently against all source files in required runtime/data, adapter, template/evaluation, script, test and reference families. Include the existing safe public documents and the relative dependencies of README, PROVENANCE, CONTRIBUTING, NOTICE and preserved historical source documents, including safe migration/timeline JSON files when linked. Preserve registry-required fenced provenance records and LICENSE/NOTICE/CITATION bytes. Include the helper and docs/distribution.md in the source inventory so rebuilding works. Classify .planning and agent workflow files as repository-internal, and classify the two exact historical audit files individually; do not exclude arbitrary docs/evidence or docs/research trees. Document every remaining excluded repository source file with a concrete internal or generated-metadata reason and reject unclassified source files. Recognized Git/virtual-environment/build/cache directories may be excluded as operational state without reading opaque contents; never use that rule to exempt files under runtime, assets or tests. Reject symlinks in selected paths and their ancestors before resolving/reading them. Scan the complete selected mapping with scan_community_safety. Apply the checkout-local import contract from the interfaces block in both the standalone helper and setup.py, including refusal of foreign already-loaded asme modules; require neither an installed package nor ambient PYTHONPATH. Preserve build_py's editable behavior, run source validation before its existing copy work and validate produced runtime/skill bytes afterwards. Add the verified sdist make_release_tree hook with pre-copy membership checks and post-copy full-tree checks; account explicitly for PKG-INFO, setup.cfg and exact egg-info metadata emitted by the backend, validating their content and rejecting unexpected generated collisions. The archive helper must examine complete tar/ZIP member lists before any extraction, scan all member bytes including metadata, validate exact source/runtime mappings, verify wheel RECORD entries and hashes, and reject unknown members. Derive project identity from existing metadata, never hard-code baseline member counts or broaden the wheel payload. Keep original generic projection/scanner implementation and CLI untouched.</action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider tests/test_distribution.py tests/test_workspace_hermes_adapter.py tests/test_release_surface.py tests/test_skill_assets.py tests/test_snapshot_package.py -q</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_distribution.py --source . --check-source</automated>
  </verify>
  <done>The RED regressions pass; full public source membership and internal classification are exhaustive; both build hooks execute the helper; all 14 omitted fixtures are declared; scanner/runtime identity and historical records remain unchanged. Every build-time failure propagates before artifact approval.</done>
</task>

<task type="auto">
  <name>Task 3: Verify real distributions and preserve post-import source accounting</name>
  <files>docs/distribution.md, .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-EVIDENCE.json, .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-SUMMARY.md</files>
  <action>Run the complete regression suite and stdlib smoke per D-06, then append verified outcomes and limitations to docs/distribution.md, whose contract and command documentation were created in Task 2. Use python3 -m build through the declared PEP 517 path to produce an actual sdist and wheel, with no manual archive substitute. Validate every archive member and byte using the helper and an independent stdlib enumeration in the evidence receipt. Record archive hashes, backend/frontend/Python versions, all member paths/hashes and exact command exit statuses. Extract only after member validation into a task-owned temporary directory; rebuild a wheel from that extracted sdist without Git or internal planning files and verify identical runtime/data and existing skill companion bytes, allowing only documented generated-metadata differences. Run the packaged smoke and relevant tests from the extracted source; report any pre-existing setuptools-dependent pytest skip separately from the mandatory real build check. Hash canonical historical/migration receipts before and after builds and assert exact equality. Never update the original import manifest to pretend MANIFEST.in, setup.py or the modified inherited test are still byte-identical to ASME. Instead bind the untouched manifest to the actual verified import commit, compare all of its destination files/modes against that Git tree, and record the complete current-versus-import diff with old/new hashes, modes and feature rationale; every changed or added source path must match this feature's authorized files. Reverify all unchanged imported files against the original receipt and demonstrate the original verifier still passes against a safely materialized immutable import snapshot, not against the intentionally changed live feature tree. Ask the independent GSD reviewer to check whole archive coverage, unknown-file refusal, generic-scanner negatives, preservation hashes, document dependency closure and the historical/current accounting. Record review findings and fixes without editing the reviewer's report. The orchestrator owns the final live merge audit, authorized commits, --no-ff merge, post-merge checks and removal of only this clean merged task-owned branch/worktree. Keep root STATE.md untouched; put this checkpoint in the quick summary and update the single WikiSkill scoped state only during the later audit/workstream integration.</action>
  <verify>
    <automated>PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 scripts/stdlib_smoke.py</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 -m build --outdir build/package-boundary-verification</automated>
    <automated>PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_distribution.py --source . --artifacts build/package-boundary-verification</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Real source and wheel artifacts plus a wheel rebuilt from the extracted source pass complete membership/byte/safety checks; full regressions and smoke have recorded outcomes. Independent review finds no unresolved defect in scope. Original receipts and historical source bytes remain verifiable, and current packaging changes have exhaustive separate accounting. The quick summary accurately distinguishes implementation, offline verification and pending integration status.</done>
</task>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Repository to public distribution | Internal operational evidence and generated state must not enter the declared source payload. |
| Manifest to filesystem | Malformed, unknown or incomplete declarations must not hide missing runtime files or escape the repository. |
| Backend output to approved archive | Generated metadata, stale files and archive path/type ambiguity must be verified before extraction or approval. |
| Historical import to current feature | Intentional modifications must not rewrite proof of original byte preservation. |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-PKG-01 | Information disclosure | Source manifest and build hooks | mitigate | Exact project membership, specific internal classification and unchanged complete-payload scan; private-runtime canary regression. |
| T-PKG-02 | Tampering | Manifest, filesystem and tar/ZIP paths | mitigate | Reject duplicate/unknown/escaping paths, symlink/hardlink/special entries, omitted required files and generated collisions before extraction. |
| T-PKG-03 | Repudiation | Archive and migration evidence | mitigate | Whole-member hashes, immutable import commit binding, complete current diff and unchanged historical receipt hashes. |
| T-PKG-04 | Tampering | Backend-generated metadata and cached source list | mitigate | Validate actual release-tree/archive membership and metadata contents, including wheel RECORD, rather than trusting MANIFEST.in alone. |
| T-PKG-SC | Tampering | Existing isolated build dependency | transfer | Use the already declared official setuptools backend; record resolved version, stop on resolution failure, and do not add or globally install dependencies. |
</threat_model>

## Multi-source coverage audit

| Source | ID | Required item | Task | Status |
|--------|----|---------------|------|--------|
| GOAL | Phase 1 criterion 4 | Separate verified package boundary while retaining exact historical evidence | 1-3 | COVERED |
| REQ | PORT-01 | Complete package content and attributable preservation after import | 1-3 | COVERED |
| RESEARCH | Existing build observations | Missing 14 evaluation assets; declared backend works; full-root generic projection rejects internal private records | 1-3 | COVERED |
| RESEARCH | Verified setuptools hooks | Validate before and after make_release_tree; preserve build_py and editable behavior | 2-3 | COVERED |
| CONTEXT | D-01 | Updated-main prerequisite, isolated branch and no root STATE mutation | Prerequisite, 3 | COVERED |
| CONTEXT | D-02 | Unchanged candidate/scanner semantics and generic docs regressions | 1-2 | COVERED |
| CONTEXT | D-03 | Exact historical/license/registry preservation | 1-3 | COVERED |
| CONTEXT | D-04 | Existing identity, dependencies and wheel payload | 1-3 | COVERED |
| CONTEXT | D-05 | Exhaustive source/archive inventory and refusal cases | 1-3 | COVERED |
| CONTEXT | D-06 | Real builds, regressions, review, integration accounting and approval boundaries | 3 | COVERED |

No quick CONTEXT.md or RESEARCH.md was supplied; settled decisions and direct source observations are recorded above. Other Phase 1 method behavior and every later WikiSkill requirement remain in their existing plans and are not claimed complete by this task.

<verification>
The new fast adversarial test group is the RED/GREEN gate. The focused inherited packaging group, complete offline regression suite, stdlib smoke, actual declared-backend build, complete archive inspection and extracted-sdist rebuild are separate required checks. Full-suite and build commands may exceed one minute; run asynchronously with regular status updates and record their complete outcomes. A skipped wheel fixture never substitutes for the real build. No test is deselected for convenience, and no scanner is weakened to obtain a pass.
</verification>

<success_criteria>
1. The actual intended source package contains the entire preserved public runtime, test/demo assets and document/license dependencies; wheel runtime/skill membership stays compatible.
2. Exact internal audit records stay canonical and byte-identical while project artifacts omit them, and generic candidate projection still refuses the same private content.
3. All adversarial cases, real archive verification, extracted-source rebuild and independent review pass, with exact evidence and historical/current source accounting.
4. Integration and safe cleanup are recorded only after the orchestrator performs and verifies them; publication, paid execution and source deletion remain outside this task.
</success_criteria>

<output>
Create .planning/quick/260906-tz7-define-and-verify-the-explicit-public-so/260906-tz7-SUMMARY.md and its EVIDENCE.json. The independent verifier owns 260906-tz7-VERIFICATION.md. The orchestrator commits planning/review artifacts and records the final scoped-state checkpoint during audit integration; do not edit root STATE.md or duplicate the WikiSkill roadmap here.
</output>
